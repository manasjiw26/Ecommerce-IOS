INTEGRATION + TESTING LEFT (tomorrow checklist)

0) Local environment
- Ensure Node LTS + npm is installed
- In Backend/ create .env from .env.example and fill:
  - SUPABASE_URL
  - SUPABASE_SERVICE_ROLE_KEY
  - (optional) GEMINI_API_KEY

1) Supabase migrations (required)
- Run:
  - Backend/migrations/new_features_schema.sql
  - Backend/migrations/v3_ambitious_features.sql
- Verify tables/columns exist:
  - registries.budget, registries.share_token, registries.theme
  - registry_items.price_snapshot, registry_items.ai_reason
  - save_for_later, registry_contributions, registry_collaborators
  - user_style_profiles, ai_conversation_history

2) Local backend run (required)
- cd Backend
- npm install
- npm start
- confirm:
  - GET http://localhost:3000/health returns {status:"ok",...}

3) Seed demo data (recommended before iOS wiring)
- node scripts/seed_demo_data.js
- copy printed values:
  - Registry ID
  - Share token
  - Demo device_id
  - Demo cart user_id (uuid)

4) End-to-end API smoke tests (required)
- Preferred (Windows):
  - node scripts/test_all_endpoints_node.js
- Optional (bash):
  - bash scripts/test_all_endpoints.sh

5) iOS integration work (required)
Registry (main demo)
- Use GET /registry/:id/dashboard as the main registry screen payload
- Wire group gifting:
  - POST /registry/:id/contribute
  - show per-item contribution progress from dashboard fields
- Wire collaborators:
  - POST/GET /registry/:id/collaborators
- Wire sharing:
  - GET /registry/:id/share-link
  - GET /registry/public/:shareToken
- Wire registry AI:
  - /ai/registry/suggest
  - /ai/registry/budget-plan
  - /ai/registry/theme
  - /ai/registry/completeness
  - /ai/registry/timeline
  - /ai/registry/gift-picker
  - /ai/registry/trending-occasion

Cart
- Wire save-for-later:
  - POST /cart/save-for-later
  - GET /cart/saved/:deviceId
  - DELETE /cart/save-for-later
- Wire move-to-registry:
  - POST /cart/move-to-registry

AI aesthetic matching (NO Gemini) — build tomorrow
- Implement palette/keyword extraction on-device (VisionKit / CoreImage)
- Call POST /ai/aesthetic-match with:
  - palette_hex, materials, finish_keywords, mood_keywords, categories, desired_items, budget

6) Render deployment (required for demo if remote)
- Connect Render to repo and service
- Set env vars in Render dashboard:
  - SUPABASE_URL
  - SUPABASE_SERVICE_ROLE_KEY
  - (optional) GEMINI_API_KEY
- Confirm Render health check hits /health

