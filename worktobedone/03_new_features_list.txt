NEW FEATURES ADDED (backend)

Database / schema
- New tables (v2):
  - registry_contributions
  - registry_collaborators
  - save_for_later
  - user_style_profiles
  - ai_conversation_history
- New columns (v2):
  - registries: budget, share_token, theme
  - registry_items: price_snapshot, ai_reason
- New tables (v3 ambitious):
  - style_quiz_results
  - occasion_plans
  - product_qa_cache
  - notification_log
  - cart_intent_events
  - curated_collections
- New columns (v3 ambitious):
  - user_style_profiles: personality_traits, avoid_categories, quiz_completed, style_tagline, emoji
  - recent_searches: parsed_intent, result_count
  - registry_contributions: is_anonymous, email

Registry API
- POST /registry/:id/budget
- GET /registry/:id/dashboard
- POST /registry/:id/contribute
- GET /registry/:id/contributions
- POST /registry/:id/collaborators
- GET /registry/:id/collaborators
- GET /registry/:id/share-link
- GET /registry/public/:shareToken
- Extended: POST /registry/:id/items now snapshots price_snapshot and accepts ai_reason

Cart API
- POST /cart/save-for-later
- DELETE /cart/save-for-later
- GET /cart/saved/:deviceId
- POST /cart/move-to-registry

AI Registry (mounted at /ai/registry)
- POST /ai/registry/suggest
- POST /ai/registry/budget-plan
- POST /ai/registry/completeness
- POST /ai/registry/theme
- POST /ai/registry/timeline
- GET /ai/registry/gift-picker
- POST /ai/registry/trending-occasion

AI (main /ai router)
- Search UX:
  - GET /ai/autocomplete
  - POST /ai/search/analytics
  - GET /ai/trending-searches
  - GET /ai/recent-searches
- Visual:
  - POST /ai/visual-search
  - POST /ai/visual-search/feedback
- Smart cart:
  - POST /ai/cart-coach
  - POST /ai/occasion-detect
  - GET /ai/resurface
- Personalization:
  - POST /ai/style-detect
  - GET /ai/style-profile
- Content generation:
  - POST /ai/gift-message
  - POST /ai/thank-you-note
  - POST /ai/product-story
- Compare & discovery:
  - POST /ai/compare-products
  - POST /ai/bundle-build
- Chat:
  - POST /ai/chat-session
  - GET /ai/chat-history
  - DELETE /ai/chat-history
- Smart search:
  - POST /ai/smart-search
  - POST /ai/price-insight
- Aesthetic suggestions:
  - POST /ai/aesthetic-suggest (Gemini vision; optional)
  - POST /ai/aesthetic-match (NO Gemini; client extracted palette/keywords)

Infra / deploy / docs
- GET /health
- Render config: Backend/render.yaml
- Docs:
  - Backend/API_DOCS.md
  - Backend/UI_EXPECTATIONS.md
  - Backend/CHANGES_MADE.md
  - Backend/TOMORROW_WORKFLOW.md
- Demo + tests:
  - Backend/scripts/seed_demo_data.js
  - Backend/scripts/test_all_endpoints.sh
  - Backend/scripts/test_all_endpoints_node.js

